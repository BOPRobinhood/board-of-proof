/**
 * @deprecated Import from `./explorer` instead.
 * Kept as a thin re-export so any leftover imports keep working.
 */
export {
  explorerAddressUrl,
  explorerTxUrl,
  truncateWalletDisplay,
} from './explorer';
