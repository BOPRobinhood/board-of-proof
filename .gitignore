# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment secrets — never commit (template stays tracked)
.env
.env*
!.env.example

# Generated by scripts/netlify-prebuild.mjs (uses VITE_API_BASE at build time)
public/_redirects

# Nested repos (own .git) — see docs/REPOS.md
/contracts/
/public/images/nfts/

# Local Supabase CLI state
supabase/.temp/

# Hand-run DB scripts — keep local only (not published with the app repo)
for_developers/sql/**/*.sql
